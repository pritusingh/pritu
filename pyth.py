def is_prime(n)
    for i in renge  (2,n):
        if n%i==0:
            return False:
                return True:
                    n=input("enter the range:")
                    for x in range(2,int(n)):
                        if is_prime():
                            print(x,"is prime")
                            else
                            print(x,"is not prime")
